# "minikube service jupyter-service -n jupyter --url" to get the url generated by minikube to access service
apiVersion: v1
kind: Service
metadata:
  namespace: jupyter
  name: jupyter-service
spec:
  type: NodePort
  ports:
    - port: 8989
      targetPort: 8888
  selector:
    app: jupyter-pods